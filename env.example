# Copy these into the Render dashboard → your service → Environment.
# Do NOT commit real values to git.

# Required
GHL_PIT=pit-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
GHL_LOCATION_ID=EY0n2rtraCf6EEUKpaEE

# Optional — lock the feed to your promo page's domain (default "*")
ALLOWED_ORIGIN=*

# Custom field IDs (get them from https://your-service.onrender.com/custom-fields)
CF_AUDIENCE_ID=
CF_START_ID=
CF_END_ID=
CF_DETAILS_ID=

# Optional filtering — use ONE of these approaches
# PROMO_PIPELINE_ID=your_promotions_pipeline_id
PROMO_KEYWORDS=promo,sale,offer,discount,deal
